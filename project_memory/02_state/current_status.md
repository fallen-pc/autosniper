# Current Status

- The sandbox is broadly runnable and documented enough for real product work.
- A full manual daily pipeline proof completed on 2026-04-18 after one runtime fix and resume. The first daily attempt exposed an Autotrader numeric missing-value crash during post-processing; after fixing it, Autotrader completed the full sweep and the remaining master/revalue/governance/outcome/health stages completed successfully.
- Treat the 2026-04-18 daily result as strong pipeline proof, but not yet as a clean unattended one-shot daily proof. Before re-enabling unattended daily automation, run one clean `scripts/scheduled_jobs.py --job daily` pass without manual resume, or add an explicit resume/guard path that makes the recovery behavior deliberate.
- Latest full-run validation status: readiness passed, project-memory check passed, full tests passed (`132` passed, one existing UTC deprecation warning), and governance passed with `33/33` observed curve tags covered and `0` monotonicity errors.
- Latest full-run data shape: Grays active links/static rows are `482`, broad active rows are `464` after WOVR filtering, sold rows are `17,089`, referred rows are `3,772`, restricted active rows are `5`, and live active snapshots remain compacted to `3` current monitoring rows.
- AI Analysis resale curve plots now show Grays sold comparable points and a sold-data best-fit line for visual context only; this does not alter valuation or bidding calculations.
- The repo now has an enforced project-memory system with manifest loading, generated machine rules, protected layers, and a task bootstrap CLI.
- Commit hygiene now enforces that meaningful code/config/UI/governance changes should carry a matching `project_memory/02_state/` update in the same commit unless an intentional exception is declared.
- Existing root memory files remain in place only as compatibility summaries during the rollout.
- Current product priorities are:
  - verify profit determination accuracy across producer, ranking, display, and calibration surfaces
  - identify what blocks adding more curves safely in Curve Builder V2
- Toyota Corolla `zre182r` hatch mapping is now split by trim at the base-curve level so `ascent` and `ascent-sport` no longer pretend to be one shared curve family.
- Corolla Ascent sedan ZRE152R and ZRE172R are now locked in as V2 base curves, with saved rows under `toyota_corolla_zre152r_sedan_auto_petrol` and `toyota_corolla_zre172r_sedan_auto_petrol` instead of their detailed matcher tags.
- Curve Builder V2 now blocks silent legacy fallback merges when mapped legacy rows disagree on the same anchor-year and km-bucket cell.
- `toyota_corolla_ascent_zre182r_hatch_auto_petrol` and `toyota_corolla_ascent-sport_zre182r_hatch_auto_petrol` now both have manual/provisional curves from Carsales-style market evidence.
- `toyota_corolla_ascent_zre182r_hatch_auto_petrol` has been rechecked against a 2026-04-12 private Carsales same-lane sample and remains accepted as-is in V2; this was not a repricing change.
- `toyota_corolla_mzea12r_hatch_auto_petrol` is now locked in as the saved V2 base curve for Corolla Ascent Sport MZEA12R hatch/auto/petrol. The existing `2019/2021/2024` price grid was retained and moved off the detailed matcher tag.
- `toyota_camry_axvh71r_sedan_auto_hybrid` is now locked in as the saved V2 base curve for Camry Ascent Hybrid AXVH71R sedan/auto/hybrid. The existing `2018/2020/2022/2024` price grid was retained and moved off the detailed matcher tag.
- `toyota_camry_ascent-sport_axvh71r_sedan_auto_hybrid` is now locked in as a separate saved V2 base curve for Camry Ascent Sport Hybrid AXVH71R sedan/auto/hybrid, using a reviewed `2018/2019/2020` anchor set from private-market evidence.
- `toyota_camry_asv70r_sedan_auto_petrol` is now locked in as the saved V2 base curve for Camry Ascent ASV70R sedan/auto/petrol, using a reviewed `2018/2020/2022` grid from private Carsales evidence plus `68` same-lane Autotrader rows.
- `toyota_corolla_zwe211r_hatch_auto_hybrid` is now locked in as the saved V2 base curve for Corolla Ascent Sport Hybrid ZWE211R hatch/auto/hybrid. The existing `2020/2021/2022` price grid was retained and moved off the detailed matcher tag.
- `toyota_corolla_zwe219r_hatch_auto_hybrid` is now locked in as the saved V2 base curve for Corolla Ascent Sport Hybrid ZWE219R hatch/auto/hybrid. The existing `2024/2025` price grid was retained and moved off the detailed matcher tag; 2025 pricing can be reviewed separately if needed.
- Corolla hybrid hatch evidence matching now uses explicit year bands: ZWE211R maps `2018-2022`, and ZWE219R maps `2023-2025`. The local Autotrader cache now has no rejected same-lane Ascent Sport Hybrid hatch rows for those two series, and the Grays sold export now has `4` matched ZWE211R rows and `0` matched ZWE219R rows while SX/ZR/Hybrid EX/Axio-style rows remain excluded.
- Autotrader evidence for the split Toyota hatch curves already exists in `autotrader_isolated/output/autotrader_recent_market_tagged.csv`, with about `24` recent-market Ascent rows and `33` recent-market Ascent Sport rows resolving cleanly to the new split base tags.
- Grays sold-tagging for the split Toyota hatch curves has now been repaired as well: the sold lane recovers `6` Ascent rows and `10` Ascent Sport rows after removing a bad year-band gate and a stale legacy tag leak.
- Stale duplicate matcher-tag curve rows have now been removed where a V2 base curve already exists, including the Toyota hatch split, Yaris, and Mazda 3 BL rows. Those match tags still resolve through the V2 group map; they no longer carry competing saved curve values.
- Corolla Ascent sedan tagging now excludes `Conquest`, so the raw Grays `toyota_corolla_ascent_petrol_auto_sedan_zre152r` lane has `42` Ascent rows and no Conquest rows after retagging.
- Camry AXVH71R now has separate saved Ascent Hybrid and Ascent Sport Hybrid lanes. Keep them separate, and treat `2025` Ascent Hybrid rows or later-year Ascent Sport Hybrid rows as deliberate extension reviews rather than automatic merges.
- All current `live_now` rows in `config/supported_curve_universe_v1.csv` now have saved V2 base-curve rows.
- The remaining Toyota hatch work is no longer basic tag alignment. It is deciding whether to keep the current manual curves as-is or run a separate repricing review against the aligned evidence lanes.
- Hyundai i30 GD work has moved from the old mixed-mainstream interpretation to an exact Active-only curve. `hyundai_i30_active_petrol_auto_hatch_gd` resolves to the base curve `hyundai_i30_gd_hatch_auto_petrol`, which now has `2012/2014/2016` anchors.
- Hyundai i30 GD tag alignment is intentionally strict: `Elite`, `Premium`, `Trophy`, `Active X`, `SE`, `SR`, and `SR Premium` are excluded from the Active curve and should be handled as separate future curves if needed.
- The local Autotrader recent-market cache currently has `57` Hyundai GD Active rows after retagging; the Grays sold source has `43` Hyundai GD Active sold rows after excluding mixed trims.
- Hyundai i30 PD work has now been rebuilt as an exact Active-only curve. `hyundai_i30_active_petrol_auto_hatch_pd` resolves to the base curve `hyundai_i30_pd_hatch_auto_petrol`, which now has `2017/2019/2022` anchors and expects `2018`, `2020`, and `2021` to interpolate.
- Hyundai i30 PD tag alignment is intentionally strict: `Active X`, `Elite`, `Premium`, `Trophy`, `SE`, `SR`, `SR Premium`, and `N Line` are excluded from the Active curve and should be handled as separate future curves if needed.
- The local Autotrader recent-market cache currently has `114` Hyundai PD Active rows after retagging; the Grays sold source has `21` Hyundai PD Active sold rows, and governance coverage sees `24` observed PD rows across active/sold/static.
- Windows scheduled tasks now point at the sandbox repo wrappers instead of the old `autosniper-main` paths.
- The hourly monitor wrapper now launches successfully under Task Scheduler and reaches live Grays update work, so the current blocker has moved from task registration to runtime stability.
- The health-report path bug is now fixed in code by writing scraper health outputs under the repo `output/health` directory instead of relying on the current working directory.
- A fresh Autotrader Playwright storage state has now been captured successfully.
- Autotrader smoke scraping now works again in `playwright-headful` mode with the refreshed storage state and returns live data (`26` rows from the VIC/Melbourne seed page in the latest smoke test).
- Headless Autotrader scraping is still not reliable with the refreshed session; current successful recovery depends on a visible browser session.
- The hourly Windows scheduled task has now been re-enabled after a successful manual smoke run.
- The daily Windows scheduled task remains disabled until a full end-to-end daily smoke run is completed.
- The scheduled hourly path has now also completed successfully by itself: the 2026-04-11 02:00 run exited with Task Scheduler `Last Result: 0`.
- A later hourly run exposed a scope bug: hourly was updating the broad live Grays active set instead of only the AI Analysis current viable set. The task was stopped, fixed, smoke-tested with `0` viable AI Analysis URLs, and re-enabled for the next scheduled run.
- AI Analysis cards now have the plumbing to show last-hour price increases from persisted valuation metadata, and hourly forced revaluation is tied to price-only changes rather than timer-only changes.
- A manual daily smoke run was attempted on 2026-04-11, but it did not complete; it was interrupted during the Grays bid/status update stage around `38/508`.
- The stale daily lock from that interrupted run was removed, and partial daily CSV churn was restored rather than treated as a valid dataset snapshot.
- Active-listing scope is intentionally split: `CSV_data/scrapers/active_vehicle_details.csv` remains the broad Active Listings / Update Master working file, while `CSV_data/restricted/active_vehicle_details_restricted.csv` and the curve-covered AI scope are narrower inputs for AI Analysis and the hourly monitor.
- As of the 2026-04-12 overnight check, the broad active set had `508` active Grays rows, restricted active had `6` rows, and AI Analysis had `5` viable active rows; the one restricted row outside AI Analysis was over the current curve km band.
- The 2026-04-12 01:00 scheduled hourly monitor run completed successfully, refreshed the `5` AI Analysis active listings, revalued them, and left scraper health green.
- A later scheduler attempt returned Windows `0x800710E0` / "operator or administrator refused the request" before the AutoSniper script started. Treat occasional missed hourly runs from laptop sleep/off/session state as acceptable if the next available hourly run resumes cleanly.
- Sandbox data snapshot commit `aed96d3` (`Update sandbox data snapshot`) is the current clean data baseline after repairing the active-link/state mismatch.
- The active dataset collapse to `2` rows was caused by stale `vehicle_state.csv` entries, not by missing active links or static details. `active_vehicle_links.csv`, `raw_vehicle_data.csv`, `normalised_data.csv`, and `vehicle_static_details.csv` all agreed on `479` current active URLs, but `vehicle_state.csv` only knew `2` of those as active before repair.
- The repaired state baseline seeded `477` missing current active-link URLs into `vehicle_state.csv` as `active` and demoted `326` stale old active rows to `static_parsed`; `update_master.py` then rebuilt `active_vehicle_details.csv` to `462` rows, with the remaining `17` active-link URLs filtered as WOVR/write-off style listings.
- After the repaired data snapshot, `scripts/readiness_smoke.py` and `scripts/governance_checks.py check` both pass.
- Hourly active snapshots are now treated as short-lived monitoring data: URL-scoped hourly runs should keep live snapshots only for the active URLs they actually monitor, while broad/daily runs may keep the broad latest-active snapshot set. Older/inactive hourly rows should live in the archive path for optional later analysis and end-of-day commits.
- Current general health: the sandbox is in good shape but not finished-production ready. The latest pushed baseline is on GitHub, core active data is healthy, hourly snapshot scope is correct, old hourly snapshot rows are archived, a full manual/resumed daily proof has completed, and readiness/project-memory/test/governance checks pass. Treat this as roughly `89/100`: strong enough for continued operational hardening, but not yet safe to rely on for real-money buying decisions without a clean unattended daily proof, scheduler reliability checks, and profit/valuation audits.
- Daily pipeline proof should use `scripts/scheduled_jobs.py --job daily-smoke`, which uses the production scheduler path, runs a full Grays link refresh to avoid narrowing the active queue, limits Grays detail scraping and Autotrader pages, then runs master update, AI revaluation, governance reporting, outcome metrics, and health reporting.
