# Next Actions

1. Map every place profit is computed, displayed, ranked, or calibrated.
2. Compare the saved split Toyota Corolla hatch `zre182r` curves against the tagged Autotrader recent-market lane and the smaller sold/Grays lane, then decide whether the manual mids need to move.
3. Keep `project_memory/02_state/` current after each meaningful work slice.
4. Use the launcher bootstrap contract for every fresh AI task so new sessions start from repo memory instead of chat recall.
5. After the Toyota hatch curves are numerically aligned, reassess the next safest Toyota curve, with `toyota_corolla_ascent_petrol_auto_sedan_zre152r` the current leading candidate.
