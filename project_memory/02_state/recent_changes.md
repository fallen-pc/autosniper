# Recent Changes

- 2026-05-27: Restored the grouped Streamlit sidebar navigation. The app must be launched through `app.py`; launching `DASHBOARD.py` directly exposes Streamlit's flat auto-discovered `pages/` list. Added `.streamlit/config.toml` `client.showSidebarNavigation = false` so the legacy auto-pages sidebar stays hidden, then rendered explicit grouped sidebar links from `shared/navigation.py` while keeping `st.navigation(..., position="hidden")` as the route manager. Restarted Streamlit on port `8501` and verified `http://localhost:8501/AI_ANALYSIS` shows the `SYSTEM`, `PIPELINE`, `VALUATION`, `AI`, `INTELLIGENCE`, and `COVERAGE` groups with expected page links and no flat `app` / `STYLE GUIDE` navigation block.

- 2026-05-26: Simplified the AI Analysis listing card layout so the support numbers no longer read as a wall of identical boxes. The card still leads with the five decision signals, but the eleven lower metrics are now grouped into `Live auction`, `Deal maths`, and `Model context` panels with stronger hierarchy. Verified `pages/6_AI_ANALYSIS.py` with `venv\Scripts\python.exe -m py_compile`, then checked `http://localhost:8501/AI_ANALYSIS` in the browser at desktop and mobile widths: the current card renders `5` decision signals, `3` metric groups, `11` metric items, no old metric boxes inside the card, no console errors, and no horizontal overflow on a `390px` viewport.

- 2026-05-26: Verified the re-registered `AutoSniper Hourly Active Monitor` after the stale-lock TTL fix. The 2026-05-25 hourly runs respected the live daily lock at `09:13` and `10:13`, then resumed cleanly after the daily completed. A later scheduled attempt was refused before reaching the wrapper (`0x800710E0`) because the task still had default battery refusal settings, so `scripts/register_automation_tasks.ps1` now registers daily/hourly tasks through ScheduledTasks with explicit working directory, `AllowStartIfOnBatteries`, `DontStopIfGoingOnBatteries`, `StartWhenAvailable`, `IgnoreNew`, and a 72-hour execution limit. Re-registered both tasks, manually triggered `AutoSniper Hourly Active Monitor`, and confirmed Task Scheduler `Last Result: 0`, `logs/scheduled/hourly_active_monitor.log` `END hourly-monitor exit=0`, no `logs/scrape.lock`, and fresh `output/health/scraper_health.json` success.

- 2026-05-24: Clarified AI action-card semantics without changing buy/watch logic. `shared/decision_policy.py` now owns operator-facing action copy, AI Analysis action tiles explain `Buy` as bid-ready and `Watch` as watch-only/not bid-ready, and Missed Opportunities uses the same shared copy for aligned labels. Verified against the current localhost AI Analysis page and today's Pajero valuation rows: the 2012 Pajero GLX `Buy` row reads bid-ready, while the 2013 Pajero GLX 4x4 `Watch` row reads watch-only. Focused validation passed with `venv\Scripts\python.exe -m py_compile shared\decision_policy.py pages\6_AI_ANALYSIS.py pages\8_MISSED_OPPORTUNITIES.py` and `venv\Scripts\python.exe -m pytest tests\test_decision_policy.py tests\test_missed_opportunities.py -q`.

- 2026-05-24: Investigated the scheduled daily/hourly stall. The expected Windows tasks were missing, so the repo wrappers had not been invoked after the last logged successful runs. Re-registered `AutoSniper Daily Pipeline` and `AutoSniper Hourly Active Monitor` from `scripts/register_automation_tasks.ps1`, then ran one catch-up daily through `scripts/run_daily.cmd`; it completed local coverage date `2026-05-23` successfully at `2026-05-24 00:25` local time. During verification, the hourly task overlapped the long daily because lock expiry used the incoming job TTL, so `scripts/scheduled_jobs.py` now evaluates stale locks using the existing lock owner's TTL. Added `test_hourly_does_not_expire_active_daily_lock`; `venv\Scripts\python.exe -m pytest -q tests\test_scheduled_jobs.py` passes.

- 2026-05-18: Committed the Streamlit UI cosmetic polish so it is no longer only a transient working-tree edit. The shared styling now uses denser page headers/cards, softer shadows, compact/hidden logo treatment on mobile, collapsed Global Filters, and readable title-case page intros. Navigation now labels the re-auction page consistently as `Re-Auction Tracker`. This was presentation-only work in `shared/styling.py`, `shared/global_filters.py`, and `shared/navigation.py`; no pipeline, valuation, scraper, or CSV business logic was changed.

- 2026-05-18: Added root `AGENTS.md` as the manual new-chat operating contract for AutoSniper Codex work. Future sessions can be told "refer to AGENTS.md" to get the repo root, memory bootstrap expectation, repo-local state-memory update rule, verification commands, commit hygiene, and push/sync boundary in one place. `docs/ai_working_agreement.md` now points manual starts to that file.

- 2026-05-18: Hardened scheduled jobs against the May 14 missing Playwright Chromium blocker. `scripts/scheduled_jobs.py` now runs a Chromium launch preflight before scheduled scrape work, self-installs `python -m playwright install chromium` by default when the browser executable is missing, supports fail-fast opt-out with `AUTOSNIPER_PLAYWRIGHT_AUTO_INSTALL=0`, and records daily preflight failures through scraper health plus `status/daily_run_state.json`. Added focused scheduler tests for auto-install, fail-fast opt-out, and install failure messaging. Verified with `venv\Scripts\python.exe -m pytest -q tests\test_scheduled_jobs.py`, full `venv\Scripts\python.exe -m pytest -q`, `scripts/readiness_smoke.py`, `scripts/governance_checks.py check`, `scripts/project_memory.py check`, and a repo-venv Chromium launch probe.

- 2026-05-18: Added a durable runtime-artifact hygiene workflow. Generated reports/audits/backups are now ignored/untracked where safe, and `scripts/git_runtime_quiet.ps1` provides a local `skip-worktree` workflow for tracked runtime CSV churn so source work can stay clean without breaking the governed CSV baseline needed by fresh clones and governance checks.
- 2026-05-14: Tightened AI Analysis card semantics in `pages/6_AI_ANALYSIS.py`. Cards now show `Action`, `Bid status`, `Margin`, `Confidence`, and `Risk` as separate signal tiles, with verdict demoted to context. Also fixed the card title fallback by coalescing merged `year/make/model/variant/location` display columns after the active-row/analysis merge, so the current Getz card renders as `2009 HYUNDAI Getz sx tb` instead of generic `Listing`.
- 2026-05-13: Started decision-model cleanup by adding a shared action-label policy. Live AI valuation and Missed Opportunities replay now use `Buy` only for currently actionable listings with bid room, expected-finish profit, current worst-case profit, and safe hard-max headroom; `Watch` is viable but constrained; `Avoid` is no-bid; `Review` is missing coverage or incomplete context. AI Analysis no longer rewrites stored `Watch` into `Watch closely`, and the old `Bid carefully` label is no longer emitted for new valuations.
- 2026-05-04: Aligned Missed Opportunities max-bid replay with AI Analysis by using the same discounted-resale cap basis for bid limits. Historical sold median still appears as expected-auction context, but it no longer silently makes historical max bids stricter than the active AI logic.
- 2026-05-04: Updated `pages/8_MISSED_OPPORTUNITIES.py` so sold historical opportunities render as AI Analysis-style cards with action/verdict pills, metric boxes, coverage/risk badges, and Overview/Curve/Costs/Condition tabs while keeping the existing sold-listing decision math unchanged.
- 2026-05-04: Pruned `project_memory/02_state/current_status.md` into a short current-truth summary. Detailed historical rollout notes stay in `recent_changes.md`; `current_status.md` is now reserved for what is true now.
- Switched live repair valuation onto the richer V2 repair engine in `shared/repair_pricing.py` instead of the tiny exact-text CSV path. Repair parsing is now case-insensitive and pattern-based, obvious damage like cracked bumpers and damaged headlights prices above `$0`, duplicate charging from exact-hit-plus-fallback is removed, and regression coverage now lives in `tests/test_repair_pricing.py`.
- Tightened repair and admin-cost valuation outputs to match the owner's actual buying workflow. Hard-avoid rows now surface their repair/risk figure instead of fake `$0`, hard-stop repairs are split into `MECHANICAL` (`$10,000`) and `STRUCTURAL` (`$8,000`) buckets, and unregistered listings no longer pay a fake full rego charge in valuation. They now carry `rego_estimate = $0`, keep the `UNREGISTERED` risk signal, and add an explicit `roadworthy_estimate` (default `$250`, configurable via `AUTOSNIPER_ROADWORTHY_ESTIMATE`) because the seller only needs to get the car roadworthy-ready, not pay the buyer's later registration bill.
- Replaced the remaining Streamlit `os.system(...)` launcher buttons with `subprocess.run(...)`, removed the shared `datetime.utcnow()` deprecation warning path, and hardened audit snapshot writing so schema changes reset malformed audit CSVs instead of appending unreadable mixed-column rows.
- Added the Hyundai Getz `SX TB` manual curve as `hyundai_getz_sx_tb_hatch_manual_petrol` with a saved `2007/2009/2011` x `30k/60k/100k/150k/200k` grid, plus the supporting V2 group-map, supported-universe, and anchor-override entries so `hyundai_getz_sx_petrol_manual_hatch_tb` resolves cleanly to the new base curve.
- Added the Hyundai ix35 `Elite LM` petrol family curve as `hyundai_ix35_elite_lm_wagon_auto_petrol` with a saved `2010/2012/2014` x `30k/60k/100k/150k/200k` grid, plus the supporting V2 group-map, supported-universe, and anchor-override entries so `hyundai_ix35_elite_petrol_auto_wagon_lm` resolves cleanly to the new base curve.
- Added a durable project-memory decision that AutoSniper is a private owner-operated tool and should be audited for safe private use rather than commercial SaaS standards, while still treating valuation correctness, data integrity, runtime reliability, and money-impacting safeguards as high priority.
- Completed a successful long-running daily retry on 2026-04-21 after an interrupted overnight attempt. The retry finished all `392` Grays live updates, completed Autotrader, rebuilt master/restricted outputs, revalued AI listings, ended with task result `0`, and the watchdog re-enabled hourly afterward. The daily lock also proved it prevents overlap: the 09:00 scheduled trigger did not start a second daily process while the retry was still running.
- Added app-level missed-daily catch-up in `scripts/scheduled_jobs.py`. Non-daily scheduled jobs now check whether the latest local-day daily run was missed and, if so, run one catch-up daily before continuing. The guard is date-based rather than backlog-based, so reconnecting after multiple missed days does not replay a queue of old daily runs, and an explicit early-morning manual daily run still counts for the current day.
- Hardened the Autotrader Streamlit helper away from raw shell strings: `pages/7_AUTOTRADER_SCRAPER.py` now builds argument lists, uses `subprocess.run(...)`, and shows stdout/stderr directly in the page.
- Added `matplotlib==3.10.8` to `requirements.txt`, widened the GitHub governance workflow to run full `pytest -q` plus readiness smoke, and fixed the scheduled-task `.cmd` wrappers so they log real timestamps.
- Repaired broad state drift in `vehicle_state.csv` and in the writer path. `scripts/update_bids.py` now demotes stale `active` state rows that are no longer in the active link queue to `static_parsed`, and the live state table was reconciled so broad active counts now match at `392`.
- Tightened the valuation surfaces after the first audit pass. Conservative margin now comes from worst-case profit where available instead of the older optimistic stored percent, so AI Analysis, Radar, and shared ranking/filter helpers stop overstating upside. The historical replay path in `shared/missed_opportunities.py` now also accepts comp median/count inputs for the expected-auction cap so it better matches the live AI decision path.
- Tightened the next valuation consistency layer as well. `TOP BUY` now gates on a conservative worst-case margin basis instead of a mid-case margin, AI valuation rows persist `expected_auction_bid_basis` alongside the raw expected finish, and AI Analysis shows the effective basis used for expected-finish profit while keeping the raw estimate visible in Bid Logic.
- Split max-bid safety from live-bid tradeability in the curve path. `hard_max_safety` now measures the final max bid itself instead of borrowing the current-bid basis when a listing is already at the ceiling, and AI Analysis now labels that as `... at max` while separately showing `no edge now` in the live status text.
- Removed the retired non-curve LLM pricing branch from `scripts/ai_listing_valuation.py`. It was already disabled and no longer had live callers, so it has been deleted instead of kept as a stale fallback that could drift from the real curve-first valuation path.
- Fixed AI Analysis max-bid display/filter drift. The page now only treats `recommended_max_bid` as a real max bid and no longer falls back to `price`, so `Hide listings without max bid` and max-bid ordering stop counting rows that never had a recommendation.
- Fixed another max-bid consistency leak in `scripts/ai_listing_valuation.py`. The legacy `expected_profit` field no longer borrows the current live bid when a listing has no edge left; it now stays tied to the final recommended max-bid basis, while `net_profit_mid` remains the live-bid actionability metric and `expected_auction_profit` remains the expected-finish metric.
- Continued the consumer cleanup after that valuation fix. `DASHBOARD.py` now computes its transparency profit bands from explicit live-profit fields instead of reading `expected_profit` directly, and `ops/outcome_tracking.py` now derives `predicted_profit` from expected-finish profit instead of the overloaded legacy max-bid field.
- Finished the remaining obvious user-facing legacy-profit consumer sweep in `pages/00_RADAR.py`. Radar now uses the shared live-profit helper instead of its own fallback chain that still included `expected_profit` directly.
- Traced the now-empty AI Analysis active scope back to the remaining restricted Corolla row and confirmed the system is behaving correctly. The row merges back to `toyota_corolla_ascent_petrol_auto_sedan_zre172r`, resolves to base curve `toyota_corolla_zre172r_sedan_auto_petrol`, and is excluded because it sits outside the current curve year and km bands rather than because of a tag or valuation failure.
- Triaged a follow-up audit note against the live sandbox instead of copying it into memory blindly. The stale blockers were rejected because readiness and governance now pass and no daily pipeline was still running; the real carry-forward tasks are Autotrader command hardening, requirements completeness, broader CI coverage, batch-wrapper timestamp fixes, tracked-artifact policy, and later cleanup of stale extra `vehicle_state.csv` active rows.
- Re-enabled the existing Windows `AutoSniper Daily Pipeline` scheduled task on 2026-04-20. It is now `Ready`, with the next run scheduled for 2026-04-21 09:00 local time; this changes the previous deferred-daily-automation state but does not remove the `Interactive only` laptop/session requirement.
- Cleaned up the AI Analysis page into an operator-facing active buying screen. `Restricted Listings` is now `Active AI Opportunities`, hidden listing counts explain active filters, no-curve rows are quieter `Diagnostics`, sidebar labels are less developer-facing, and card/Bid Logic wording now separates `Profit if bought now`, `Expected finish`, `Profit at expected finish`, and `Max bid limit`.
- Tightened valuation display and dashboard ranking after the first profit audit pass. `repair_estimate` is now persisted, repair cost is subtracted from displayed expected/worst profit, `$0` max bids are preserved as real zero values, and Live Opportunities now ranks only safe active listings with positive worst-case profit and bid room above the current price.
- Added shared valuation display/ranking helpers plus regression tests for zero-value parsing, safe live-opportunity filtering, worst-case profit ranking, and repair-cost profit accounting.
- Tightened Radar and no-edge valuation accounting. Radar now ranks from worst-case profit first and pushes non-tradeable rows down instead of promoting traps/avoid/no-edge rows; AI Analysis now calculates no-edge profit at the live current bid when the current bid has already passed the recommended max.
- Aligned Missed Opportunities decision math with the live AI Analysis path by moving the historical max-bid/profit reconstruction into `shared/missed_opportunities.py`. It now applies the expected-auction cap, handles repair deductions consistently, separates repair cost from risk buffer, and has regression coverage for repair and no-repair scenarios.
- Added a repeatable valuation calibration report at `scripts/calibration_report.py`, backed by `shared/calibration.py` and tests. The report writes detail/summary outputs under `output/calibration/` and classifies sold rows into groups such as profitable within bid, overbid risk, curve too conservative, correct pass, and not covered.
- Added a dedicated Valuation Calibration Streamlit page in the Intelligence section. The page surfaces the calibration report interactively with repair-cost toggling, reason/make/model/curve filters, priced-out winner review, overbid-risk review, and full filtered CSV export.
- Updated the operating cost model for the user's actual VIC buying strategy: default transport is now `$200`, the default operating state is `VIC`, and interstate listings are marked as out-of-operating-state/non-buyable for valuation and calibration instead of being treated as normal opportunities with a transport surcharge.
- Refreshed active AI valuations under the VIC-local rules. Current curve-covered active scope is `3` rows: `2` NSW rows now show `Avoid`, `$0` max bid, and `$0` expected profit; `1` VIC Hyundai i30 remains a `Conditional Flip`.
- Expected auction price now prefers matching Grays sold-auction median when at least `3` sold comps exist, while hard max bid remains separate. The VIC Hyundai i30 now uses `10` historical sold comps, expected auction `$5,850`, expected worst-profit `$2,501`, and hard max `$6,834`.
- AI valuation output now separates live-auction labels from the old single verdict: current-bid profit label/score, expected-finish profit label, hard-max safety, bid status, flip difficulty/reasons, and action label. The current VIC Hyundai i30 reads `Watch`, `Cheap`, `Strong` current profit, `Good` expected-finish profit, `Conditional` hard-max safety, and `Medium` flip difficulty.
- Fixed AI Analysis page curve-coverage display so detailed listing tags resolve to their base curve tags before coverage filtering. The page should now show `3` curve-covered active rows and `2` no-curve rows instead of incorrectly treating all `5` active restricted rows as no-curve.
- Moved the AI Analysis no-curve diagnostics below the curve-covered listing cards, with the no-curve table collapsed by default, so watchable opportunities are visible first.
- Fixed the second AI Analysis display filter that was still comparing detailed listing tags directly against base curve tags. Local reproduction now keeps all `3` curve-covered active rows in the card display path.
- Recorded the scheduler decision after explaining Windows `Interactive only`: daily automation is not being re-enabled in this work slice. The clean daily pipeline is proven, but the user will handle laptop power/sleep/logged-in session setup later before relying on scheduled daily runs.
- Ran a clean full daily pipeline proof on 2026-04-18 via `scripts/scheduled_jobs.py --job daily` with no manual resume. It completed Grays link extraction, static detail refresh, all `482` Grays bid/status updates, Autotrader full sweep, master update, AI revaluation, governance reporting, outcome metrics, health reporting, and lock cleanup.
- Clean daily Autotrader result: `414` pages fetched, `8,316` latest-snapshot rows, `16,430` total unique Autotrader rows, and listing-history counts of `listed=70`, `relisted=86`, `price_changes=21`, `sold=155`.
- Clean daily validation passed after the run: `scripts/readiness_smoke.py`, `scripts/project_memory.py check`, `pytest -q` (`132` passed, one existing UTC deprecation warning), and `scripts/governance_checks.py check --allow-dataset-change "CSV_data/**"` all passed. Governance still covers all `33` observed tags with `0` monotonicity errors.
- Ran a full manual daily pipeline on 2026-04-18. The first attempt completed the broad Grays stages but failed during Autotrader post-processing with `ValueError: cannot convert float NaN to integer`; `_to_int_or_blank` in the isolated Autotrader scraper now treats missing numeric values such as `NaN`/`pd.NA` as blank instead of crashing.
- Resumed the Autotrader full sweep after the fix. It completed `415` pages, wrote `8,315` latest-snapshot Autotrader rows, maintained `16,363` unique rows in `first_page_results.csv`, and updated listing history with `listed=68`, `relisted=40`, `price_changes=47`, and `sold=156`.
- Finished the remaining daily downstream stages after the Autotrader resume: `update_master`, AI revaluation, governance report bundle, outcome metrics, scraper health, and daily metrics all completed. Final broad Grays counts were `active_vehicle_links=482`, `vehicle_static_details=482`, `active_vehicle_details=464`, `sold_cars=17,089`, and `referred_cars=3,772`; active hourly snapshots remained compacted to `3` current rows as intended.
- Verified the completed full-run state with `scripts/readiness_smoke.py`, `scripts/project_memory.py check`, `pytest -q`, and `scripts/governance_checks.py check --allow-dataset-change "CSV_data/**"`. Results: readiness passed, memory check passed, `132` tests passed with one existing UTC deprecation warning, governance covered all `33` observed tags with `0` monotonicity errors.
- Added active snapshot retention: hourly `active_snapshots.csv` now compacts to the latest row per still-active listing, while older rows and finished-listing rows are archived under `CSV_data/archives/active_snapshots/` for later analysis and end-of-day commits.
- Tightened hourly snapshot retention so URL-scoped hourly runs keep live snapshots only for the active URLs they actually monitored, instead of repopulating the live snapshot file from the whole broad active dataset.
- Updated sold-training preparation so archived active snapshots can still be read when generating later analysis/training datasets.
- Recorded the current project-health summary: sandbox health is good and improving, but remaining blockers for production readiness are daily end-to-end proof, Autotrader/session stability, scheduler reliability, and profit/valuation verification.
- Added a controlled `daily-smoke` scheduled job path so the pipeline can be proven end-to-end with a full Grays link refresh plus small detail and Autotrader limits before attempting a full unattended daily run.
- The first `daily-smoke` attempt reached Grays detail scraping, AI-scope bid update, one-page Autotrader scraping, master update, restricted rebuild, AI revaluation, and governance output, then exposed an outcome-metrics crash when the optional verdict CSV was empty.
- Repaired and committed the sandbox CSV data snapshot as `aed96d3` (`Update sandbox data snapshot`).
- Diagnosed the `active_vehicle_details.csv` collapse to `2` rows: `vehicle_state.csv` was stale and only overlapped the current `479` active-link/static/raw/normalised URLs on `2` URLs.
- Repaired `vehicle_state.csv` by seeding `477` missing active-link URLs as `active` and demoting `326` stale old active rows to `static_parsed`, then rebuilt master/restricted datasets with `scripts/update_master.py`.
- Final repaired counts: `active_vehicle_links.csv=479`, `vehicle_state.csv active=479`, `active_vehicle_details.csv=462`, `active_vehicle_details_restricted.csv=5`; the `17` link/detail gap is expected WOVR filtering.
- Verified the repaired snapshot with `scripts/readiness_smoke.py` and `scripts/governance_checks.py check`; both passed before the data commit.
- Added a permanent curve-grid regression guard: every `live_now` supported base curve must have saved rows, only supported base tags may carry saved rows, every anchor must use the standard `30000/60000/100000/150000/200000` km buckets, price bands must stay ordered, and `price_mid` must decline as kilometres increase.
- Locked in `toyota_camry_asv70r_sedan_auto_petrol` as the V2 base curve for Camry Ascent ASV70R sedan/auto/petrol using a `2018/2020/2022` anchor grid from the reviewed private Carsales slice plus `68` same-lane Autotrader rows; the single Grays sold row was treated as non-contributory valuation evidence.
- Added and locked in `toyota_camry_ascent-sport_axvh71r_sedan_auto_hybrid` as a separate V2 base curve for Camry Ascent Sport Hybrid AXVH71R sedan/auto/hybrid using a `2018/2019/2020` anchor set from reviewed private-market evidence; this was intentionally kept separate from the plain Ascent Hybrid lane.
- Locked in `toyota_camry_axvh71r_sedan_auto_hybrid` as the V2 base curve for Camry Ascent Hybrid AXVH71R sedan/auto/hybrid by moving the existing `2018/2020/2022/2024` saved rows off the detailed matcher tag and onto the base tag without changing prices.
- Locked in `toyota_corolla_mzea12r_hatch_auto_petrol` as the V2 base curve for Corolla Ascent Sport MZEA12R hatch/auto/petrol by moving the existing `2019/2021/2024` saved rows off the detailed matcher tag and onto the base tag without changing prices.
- Fixed Corolla hybrid hatch evidence matching: Autotrader `2018/2019` Ascent Sport Hybrid hatch rows now map into ZWE211R, Autotrader `2023` rows now map into ZWE219R, one stale Grays sold `2019` row now maps into ZWE211R, and SX/ZR/Hybrid EX/Axio-style rows remain rejected.
- Locked in `toyota_corolla_zwe219r_hatch_auto_hybrid` as the V2 base curve for Corolla Ascent Sport Hybrid ZWE219R hatch/auto/hybrid by moving the existing `2024/2025` saved rows off the detailed matcher tag and onto the base tag without changing prices.
- Added a visual Grays sold-comparable overlay to the AI Analysis resale curve plots, including a dashed best-fit line through the sold data; this is display-only and does not change curve estimates, bidding, or confidence logic.
- Locked in the Corolla Ascent ZRE152R and ZRE172R sedan/auto/petrol curves as V2 base curves by moving their saved rows onto `toyota_corolla_zre152r_sedan_auto_petrol` and `toyota_corolla_zre172r_sedan_auto_petrol` without changing prices.
- Locked in `toyota_corolla_zwe211r_hatch_auto_hybrid` as the V2 base curve for Corolla Ascent Sport Hybrid ZWE211R hatch/auto/hybrid by moving the existing `2020/2021/2022` saved rows off the detailed matcher tag and onto the base tag without changing prices.
- Marked `toyota_corolla_ascent_zre182r_hatch_auto_petrol` as reviewed/accepted in V2 after a 2026-04-12 private Carsales check of `30` same-lane Ascent ZRE182R hatch/auto/petrol listings; no price-grid change was made because the current saved values still fit the evidence closely enough.
- Recorded the Camry AXVH71R boundary decision: keep `toyota_camry_ascent_hybrid_auto_sedan_axvh71r` as an exact Ascent Hybrid lane, do not merge Ascent Sport Hybrid into it, and treat `2025` Ascent Hybrid rows as a separate curve-extension review.
- Removed stale duplicate matcher-tag curve rows where a V2 base curve already exists. The affected matcher rows were `toyota_corolla_ascent_petrol_auto_hatch_zre18x`, `toyota_corolla_ascent-sport_petrol_auto_hatch_zre18x`, `toyota_yaris_yr_petrol_auto_hatch_ncp90r`, and `mazda_3_neo_petrol_auto_hatch_bl`; these now resolve to their base curves instead of carrying competing saved values.
- Tightened Corolla Ascent tagging so `Conquest` is excluded from the Ascent lanes. After retagging, the raw Grays `toyota_corolla_ascent_petrol_auto_sedan_zre152r` sold lane contains `42` Ascent rows and no Conquest rows.
- Rebuilt the Hyundai i30 PD curve as an exact `Active` hatch/auto/petrol slice rather than a broad PD hatch lane. The saved base curve `hyundai_i30_pd_hatch_auto_petrol` now uses `2017/2019/2022` anchors, and the stale duplicate matcher-tag curve rows were removed so the app resolves to one source of truth.
- Tightened Hyundai PD tagging so `Active X`, `Elite`, `Premium`, `Trophy`, `SE`, `SR`, `SR Premium`, and `N Line` no longer map into `hyundai_i30_active_petrol_auto_hatch_pd`.
- Rebuilt Grays restricted datasets after the Hyundai PD tag fix: the Hyundai PD Active sold lane now has `21` raw Grays sold rows and the governance coverage report sees `24` observed rows across active/sold/static for the matcher tag.
- Retagged the local Autotrader recent-market cache after the Hyundai PD tag fix; the Hyundai PD Active Autotrader lane now contains `114` rows.
- Rebuilt the Hyundai i30 GD curve as an exact `Active` hatch/auto/petrol slice rather than the older mixed-mainstream slice. The saved base curve `hyundai_i30_gd_hatch_auto_petrol` now uses `2012/2014/2016` anchors, and the stale duplicate matcher-tag curve rows were removed so the app resolves to one source of truth.
- Tightened Hyundai GD tagging so `Elite`, `Premium`, `Trophy`, `Active X`, `SE`, `SR`, and `SR Premium` no longer map into `hyundai_i30_active_petrol_auto_hatch_gd`.
- Rebuilt Grays restricted datasets after the Hyundai tag fix: the Hyundai GD Active sold lane now has `43` raw Grays sold rows and the governance coverage report sees `47` observed rows across active/sold/static for the matcher tag.
- Retagged the local Autotrader recent-market cache after the Hyundai tag fix; the Hyundai GD Active Autotrader lane now contains `57` rows and all sampled rows are `Active` variants, not `Elite`, `Premium`, or `Trophy`.
- Added persistent price-change metadata for active AI valuation rows and updated the AI Analysis cards to show whether each listing's current price increased in the last hour. Hourly revaluation now uses price-only changes for forced refreshes instead of treating countdown-only changes as price movement.
- Found and fixed an hourly monitor scope bug: the scheduled hourly bid refresh was reading the broad live Grays `active_vehicle_details.csv` file instead of the AI Analysis eligible active scope. Hourly is now wired to the same curve-covered restricted active scope used by AI Analysis before it updates bids, and a fast smoke run with `0` viable AI Analysis URLs exited without touching broad Grays rows.
- Fixed Toyota Corolla `zre182r` sold-tagging so valid Ascent and Ascent Sport Grays auto/CVT hatch rows are no longer rejected just because the saved curve anchors start later than the real model years.
- Re-tagged `CSV_data/scrapers/sold_cars.csv` and rebuilt the restricted sold outputs, which moved the Toyota hatch sold counts from `1` to `6` for Ascent and from `4` to `10` for Ascent Sport.
- Removed the stale `toyota_corolla_hatch_petrol_auto_ascent_fwd_2013_2015` leakage from the Toyota hatch sold lane.
- Added a durable rule that evidence-alignment work is not repricing work, so future tag-audit tasks do not quietly change saved curve values without explicit approval.
- Confirmed that the split Toyota Corolla hatch base curves already resolve into the tagged Autotrader evidence lane: about `24` recent-market Ascent rows and `33` recent-market Ascent Sport rows now map cleanly to the new `zre182r` split base tags.
- Confirmed that the main governed active CSVs are still Grays-dominated, so Toyota hatch active alignment should be checked from `autotrader_isolated/output/autotrader_recent_market_tagged.csv` rather than from `CSV_data/scrapers/active_vehicle_details.csv`.
- Saved a manual/provisional `toyota_corolla_ascent-sport_zre182r_hatch_auto_petrol` curve using a `2014/2016/2018` anchor set based on Carsales-style Ascent Sport evidence.
- Added a durable project-memory decision that a curve is only complete when the anchor grid is filled and the tag lines up with the intended Autotrader and sold/Grays evidence lanes.
- Started a manual/provisional rebuild of `toyota_corolla_ascent_zre182r_hatch_auto_petrol` using a simpler `2013/2015/2018` anchor set driven by Carsales-style Ascent evidence instead of the old grouped hatch fallback.
- Toyota Corolla `zre182r` hatch support is now split into separate `ascent` and `ascent-sport` base curves instead of one grouped base that hid a real trim premium.
- Commit hygiene now blocks meaningful code/config/UI/governance changes that do not also stage a `project_memory/02_state/` update, unless `AUTOSNIPER_STATE_MEMORY_OPTIONAL=1` is set intentionally for an exception.
- Toyota Corolla hatch follow-up is now focused on operator visibility rather than automatic resolution: the V2 page already blocks conflicting legacy fallback rows correctly, so the next safe work is to explain those conflicts more clearly without choosing a winning legacy branch in code.
- Curve Builder V2 now summarizes conflicting legacy fallback cells before showing the raw rows, including which legacy source tag is lower or higher on `price_mid` and the size of that gap. This keeps Toyota hatch conflict handling manual but much faster to inspect.
- The repo already had first-generation memory files (`task_plan.md`, `findings.md`, `progress.md`, and `docs/ai_working_agreement.md`).
- Those files have now been folded into a layered memory architecture under `project_memory/`.
- Generated machine rules are now refreshed from authoritative code instead of being manually copied.
- Pre-commit and CI are being wired to fail when required memory is missing, stale, or edited in protected layers without approval.
- Curve Builder V2 now uses a dedicated legacy-seed helper that detects conflicting legacy curve rows before fallback seeding.
- When conflicting legacy rows are found, the V2 page now shows the conflict and starts from a blank grid instead of silently mixing old curves.
- Tests now lock in the Toyota-style conflict case so the silent-merge failure mode does not return unnoticed.
- The repo now has a PowerShell AI-task launcher wrapper at `scripts/start_ai_task.ps1` that runs memory bootstrap first and aborts if it fails.
- The launcher can now also prepare a combined startup prompt, copy it to the clipboard, and open the Codex app for manual paste-based starts.
- Repointed the Windows scheduled-task wrappers to the sandbox repo and hardened the wrapper scripts so they choose either `.venv`, `venv`, or fallback `python`, create `logs/scheduled`, and run from the repo root.
- Fixed the scraper health-report path so scheduled runs write to the repo `output/health` folder instead of failing on a relative `output` path under Task Scheduler.
- Confirmed that the hourly monitor now launches and reaches live Grays update work under the sandbox wrapper instead of failing immediately on startup.
- Refreshed the Autotrader Playwright storage state manually and confirmed that a one-page VIC/Melbourne smoke scrape succeeds in `playwright-headful` mode, producing `26` rows with HTTP `200`.
- Confirmed that the same refreshed Autotrader session still fails in headless mode with `403`, so current recovery is session-valid but not headless-stable.
- Stopped the live hourly monitor run and intentionally disabled both Windows scheduled tasks so the repo stays clean and paused between sessions.
- Ran a full manual hourly monitor smoke test to completion: it exited `0`, touched `170` listings, refreshed `active_vehicle_details.csv` and `vehicle_state.csv`, and wrote a fresh `output/health/scraper_health.json`.
- Re-enabled the hourly Windows scheduled task after that successful manual proof, while keeping the daily task disabled.
- Confirmed the re-enabled scheduled hourly path works: the 2026-04-11 02:00 scheduled run exited with Task Scheduler `Last Result: 0` and wrote a fresh scraper health report.
- Attempted a manual daily smoke run on 2026-04-11; it was interrupted during the Grays bid/status update stage around `38/508`, before Autotrader or the later daily stages ran.
- Removed the stale daily lock left by the interrupted run and restored the partial daily CSV churn so it is not mistaken for a valid completed snapshot.
- Repaired the active-listing data split after an interrupted broad Grays run: rebuilt restricted datasets from the current `508` broad active rows, restored `6` restricted active rows, and confirmed the AI Analysis/hourly scope is `5` curve-covered active listings.
- Confirmed the Active Listings page should keep reading the broad `active_vehicle_details.csv` file, while AI Analysis and hourly monitoring should use the narrower restricted/curve-covered active scope.
- Confirmed the 2026-04-12 01:00 scheduled hourly run completed successfully against the `5` AI Analysis listings and refreshed/revalued them without broad hourly churn.
- Recorded the operational scheduler rule: laptop off/asleep/session-refused hourly runs are not automatically scraper failures; investigate only if the next available hourly run does not resume cleanly while the machine is awake and logged in.
- Added new canonical-tag coverage for a first mainstream expansion batch: `toyota_camry_altise_petrol_auto_sedan_asv50r`, `mazda_cx5_maxx-sport_diesel_auto_wagon_ke`, `hyundai_ix35_se_petrol_auto_wagon_lm`, `hyundai_ix35_elite_petrol_auto_wagon_lm`, `hyundai_getz_sx_petrol_auto_hatch_tb`, and `hyundai_getz_sx_petrol_manual_hatch_tb`.
- Fixed two canonical-tag parser gaps while adding those lanes: short series codes like `KE`, `LM`, and `TB` now resolve correctly, and model-like tokens such as `ix35`, `cx5`, and `cx9` no longer get misread as series codes.
- The new tagging rules are covered by regression tests and, against the current active file, recover `10` rows from `UNCLASSIFIED` into valid tags: `3` Camry Altise, `2` CX-5 diesel, `2` Getz manual, `1` Getz auto, `1` ix35 SE, and `1` ix35 Elite.
- Added the first curve for that new mainstream batch: `toyota_camry_asv50r_sedan_auto_petrol` with a saved `2013/2015/2017` x `30k/60k/100k/150k/200k` grid, plus the supporting V2 group-map, supported-universe, and anchor-override entries so `toyota_camry_altise_petrol_auto_sedan_asv50r` resolves cleanly to the new base curve.
- Added the second curve for that mainstream expansion batch: `mazda_cx5_maxx-sport_ke_wagon_auto_diesel` with a saved `2012/2014/2016` x `30k/60k/100k/150k/200k` grid, plus the supporting V2 group-map, supported-universe, and anchor-override entries so `mazda_cx5_maxx-sport_diesel_auto_wagon_ke` resolves cleanly to the new base curve.
- The CX-5 diesel lane is intentionally conservative and family-based. It currently spans early `KE` and `KE Series 2` pricing inside one Maxx Sport diesel base curve rather than splitting those sub-eras into separate saved curves.
- Added the Hyundai ix35 `SE LM` petrol curve as `hyundai_ix35_se_lm_wagon_auto_petrol` with a saved `2013/2014/2015` x `30k/60k/100k/150k/200k` grid, plus the supporting V2 group-map, supported-universe, and anchor-override entries so `hyundai_ix35_se_petrol_auto_wagon_lm` resolves cleanly to the new base curve.
- The old note that kept `ix35 Elite LM` out of the curve set is now superseded. The lane now has its own conservative family-level curve rather than being merged into `SE LM`.
- Added the Hyundai Getz `SX TB` automatic curve as `hyundai_getz_sx_tb_hatch_auto_petrol` with a saved `2008/2010/2011` x `30k/60k/100k/150k/200k` grid, plus the supporting V2 group-map, supported-universe, and anchor-override entries so `hyundai_getz_sx_petrol_auto_hatch_tb` resolves cleanly to the new base curve.
- The old note that kept Hyundai Getz manual out of the curve set is now superseded. The lane now has its own conservative manual curve rather than being collapsed into the automatic one.
- Rebuilt the restricted datasets and re-ran active AI valuations after the four-curve expansion. AI Analysis active scope now has `6` curve-covered rows: `2` Camry Altise, `2` CX-5 Maxx Sport diesel, `1` ix35 SE, and `1` Getz SX automatic.
- The first live outcomes from that wider scope are useful rather than uniformly good. The `2013` Camry Altise and `2014` ix35 SE now show `Conditional Flip` / `Watch`, the `2009` Getz SX automatic shows `Conditional Flip` / `Bid carefully`, and the remaining covered rows are priced but currently `Avoid` because the live bid has already removed the edge.
- Rechecked the older audit snapshot against the live sandbox on 2026-04-24 instead of carrying it forward blindly. Readiness now passes, governance now passes, active/state counts are aligned again, and `active_vehicle_details.csv` includes the governed `date_sold` column, so the old launch-blocker story is no longer current.
- Traced the 2026-04-24 `Lock busy; skipping daily run.` event to the new missed-daily catch-up path rather than a stuck scraper. A non-daily scheduled job hit catch-up at `09:00`, started the daily run itself, and the normal `09:00` daily task then exited cleanly when it found the lock already held.
- Patched that scheduler overlap in `scripts/scheduled_jobs.py`. Non-daily jobs still trigger catch-up for real missed daily runs, but they now skip same-day catch-up during a configurable post-schedule grace window so the normal `09:00` daily task gets priority instead of racing the hourly task for the same lock.
- 2026-04-27: Loosened the non-hard-avoid repair gate. Mechanical and structural hard-stops still force `Avoid`, but moderate repair totals now stay in `Marginal` longer instead of auto-escalating to `Avoid`. High-km listings above curve range remain excluded from AI valuation.
- 2026-04-27: Fixed the lifecycle state-machine bug that permanently locked listings in terminal states (`sold` / `referred` / `withdrawn` / `dead_url`) even when fresh live-auction evidence returned. Terminal rows can now reopen to `active` on real live evidence, and the active materialized view was rebuilt so readiness checks pass again.
- 2026-04-29: Added a compact AI decision-change ledger in `CSV_data/ai/listing_decision_events.csv`. The valuation save path now records meaningful transitions per URL such as verdict/action changes, material max-bid or repair-estimate moves, bid-status flips, and risk/coverage changes, while ignoring small numeric noise.
- 2026-04-29: Tightened AI shortlist filtering in `ops/active_monitor.py` so completed rows, no-price rows, and WOVR/repairable rows are dropped before active valuation. Rebuilt the materialized master/restricted views afterward; readiness now passes again, and the remaining restricted active rows are both intentionally out of curve km range rather than false shortlist candidates.
- 2026-04-29: Cleaned up AI Analysis wording in `pages/6_AI_ANALYSIS.py`. Cards now lead with the action, treat the verdict as supporting context, and rename current/expected profit labels into clearer margin-strength wording (`High margin`, `Good margin`, etc.) to reduce mixed-signal reading on rows like `Watch closely` plus `Marginal`.
- 2026-05-05: Found a sold-price integrity failure where lifecycle `current_price` / visible bid values could be materialized into `sold_cars.csv` as final sale prices. Added guarded state fields (`final_sale_price`, `final_sale_date`, `sale_price_source`), stopped `update_bids.py` from treating visible price+bids or ended+bids as sold evidence, and changed `update_master.py` so sold history can only use verified final sale price fields. Sold-state rows without verified final price now go to `sold_price_pending.csv` for review instead of polluting sold history.
- 2026-05-06: Started the sold-history rebuild from newest rows backwards. `scripts/rebuild_sold_dataset.py` now supports `--newest-first` and `--offset` chunking, and it rejects re-scraped rows whose date field is still a live closing time rather than a verified sold date. Rebuilt source offsets `0` through `1400`; `CSV_data/archives/sold_cars_rescraped.csv` now has `1,431` accepted rows, `0` duplicate URLs, and `0` live-closing rows. Comparison against current `sold_cars.csv` shows a major bad-price window around source-from-end `900-1199`: chunk `900` has `86/99` mismatches, chunk `1000` has `85/95`, chunk `1100` has `56/95`, while chunk `1200` is clean (`0/97`), chunk `1300` has one isolated mismatch (`2013 Camry Atara S`, `$3,109 -> $5,212`), and chunk `1400` is clean (`0/97`). Known Camry fixes are confirmed (`$209 -> $10,500`, `$409 -> $9,350`). Resume from `--offset 1500` only if more boundary confidence is needed.
- 2026-05-06: Added `scripts/repair_sold_prices_from_rescrape.py` to produce a surgical sold-price repair preview from the rebuilt sold archive. It generated `CSV_data/archives/sold_price_repair_report.csv` with `234` valid price repairs and `CSV_data/archives/sold_cars_repaired_preview.csv` with the original `17,104` rows preserved, `0` duplicate URLs, `0` blank prices, and `0` validator drops. The two known Camrys are repaired in the preview (`$209 -> $10,500`, `$409 -> $9,350`). The live `CSV_data/scrapers/sold_cars.csv` has not been replaced yet.
- 2026-05-06: Promoted the repaired sold preview to live `CSV_data/scrapers/sold_cars.csv` after backing up the previous file to `CSV_data/archives/sold_backups/sold_cars_before_repair_2026-05-06.csv`. Rebuilt restricted datasets afterward: `active=7`, `sold=429`, `restricted_group_map=17,404`. The Missed Opportunities input merge now shows the known Camrys correctly: `2016 Camry Altise ASV50R = $9,350`, `2018 Camry Ascent ASV70R = $10,500`, both with valid canonical tags.
- 2026-05-06: Refreshed downstream calculations after the sold-price repair. Forced AI Analysis active revaluation for the current eligible scope (`4` rows evaluated, `0` dropped coverage), rebuilt governance reports (`45/45` observed tags covered, `0` missing, `0` monotonicity issues), and recomputed outcome metrics so cached reports line up with the corrected sold prices.
- 2026-05-06: Added `docs/cloud_scraper_setup.md` as the first cloud migration runbook. The recommended path is a small Windows VPS that mirrors `scripts/scheduled_jobs.py`, with the CSV backup/sync strategy chosen before production cutover.
- 2026-05-06: Chose OneDrive-style zip backups as the first cloud data strategy and added `scripts/backup_runtime_data.ps1`. The script backs up runtime CSV/report/log folders to `AUTOSNIPER_BACKUP_DIR` or `-BackupDir`, preserves restore-friendly paths inside the zip, skips Autotrader session files unless `-IncludeAutotraderSession` is passed, and was smoke-tested locally with a `28.65 MB` runtime archive.
- 2026-05-06: Wired runtime backups into the full daily scheduled runner. After `run_daily_pipeline()` succeeds, `scripts/scheduled_jobs.py --job daily` now calls `scripts/backup_runtime_data.ps1` only when `AUTOSNIPER_BACKUP_DIR` is configured; otherwise it skips cleanly. `AUTOSNIPER_BACKUP_INCLUDE_AUTOTRADER_SESSION=1` opts into backing up Autotrader session files. `tests/test_scheduled_jobs.py` covers the skip/configured/session cases.
- 2026-05-06: Added `scripts/verify_runtime_backup.ps1` to safely verify runtime backup zips without overwriting live files. It extracts to a temp folder, checks key files, loads the main CSVs, prints row counts, and cleans up. Verified the local test backup successfully: `sold_cars=17,104`, `sold_cars_restricted=429`, `ai_listing_valuations=258`, with status/health files present.
- 2026-05-06: Cloud scraper migration was put on hold for budget reasons after reviewing cheap Windows VPS options. The local/laptop scheduled path remains the active production path for now.
- 2026-05-06: Finished the interrupted full daily run manually. Removed the stale daily lock from the interrupted catch-up, resumed `scripts/update_bids.py --skip-master` from `logs/update_resume.json`, completed the remaining Grays bid/status URLs, ran Autotrader successfully (`8,908` rows across `440` pages, listing history updated: `listed=419`, `relisted=178`, `price_changes=638`, `sold=483`), rebuilt master/restricted datasets (`active=6`, `sold=429`, `map=17,352`), revalued current AI scope (`5` rows, `0` dropped coverage), rebuilt governance/outcomes, and explicitly marked the May 6 daily coverage as successful. Readiness and governance checks pass; health report has no stale datasets.
- 2026-05-06: Added `scripts/generate_opportunity_lanes.py` and generated `output/opportunity_lanes.csv` / `.md` from the fresh Autotrader market scrape versus repaired Grays sold history. The report ranks candidate vehicle lanes by Autotrader median, Grays sold median, estimated costs, estimated margin, support status, evidence counts, and median-year gap; year-mismatched lanes are separated so newer retail rows are not falsely compared to much older auction sales. Current output has `586` matched lanes: `104` strong_review, `88` review, `303` thin_evidence, `89` year_mismatch, and `2` watch.
- 2026-05-06: Dug into the opportunity-lane report and saved `output/opportunity_lanes_practical_shortlist.csv` with the stricter review slice: `strong_review/review`, at least `8` Autotrader rows, at least `8` sold rows, median year gap `<=2`, and median odometer gap `<=70k`. This leaves `28` more practical lanes for manual curve/tag review; the top cleaner candidates include Toyota Hilux SR diesel auto, Subaru Outback/Forester, Isuzu D-Max, BMW X5/X3, Nissan Pathfinder, Audi A4/Q3, Toyota Kluger/Aurion, Nissan Qashqai, Ford Ranger XL 2.2, Toyota Yaris, Nissan Micra, and Corolla Ascent Seca.
- 2026-05-06: Started the ute deep-dive with Toyota Hilux SR diesel auto and added `scripts/inspect_hilux_ute_lanes.py`, writing `output/ute_lanes/hilux_sr_diesel_auto_evidence.csv` and summary. The cleanest first ute lane is `4x4 dual cab/crew cab chassis`: `66` Autotrader rows at median `2020`, `$42,975`, `106,727 km` and `127` Grays sold rows at median `2020`, `$25,100`, `118,824 km`. Sold evidence splits into `119` newer `GUN126R` rows and `8` older `KUN26R` rows, so the first curve should likely be the 2015+ `GUN126R` lane rather than a generic Hilux SR curve.
- 2026-05-07: Added the first ute curve/tag lane: `toyota_hilux_sr_diesel_auto_cab_chassis_gun126r` maps to `toyota_hilux_sr_gun126r_cab_chassis_auto_diesel`. The lane is intentionally narrow: Hilux SR, GUN126R, diesel, auto, 4x4 dual/crew cab chassis; it excludes SR5/Workmate/Rogue/Rugged, manual, 4x2/Hi-Rider, single/extra cab, and pickup/tub rows. Added a provisional 2016/2018/2020/2022 x 30k/60k/100k/150k/200k resale grid from Autotrader retail evidence, rebuilt restricted datasets, and confirmed `119` repaired Grays sold rows now flow into the restricted sold/Missed Opportunities evidence. The curve is explicitly marked provisional and needs Carsales/private retail validation before being treated as fully reviewed. There is no current active Hilux in the restricted active set, so AI Analysis did not gain an active Hilux card yet.
- 2026-05-07: Reworked the Hilux SR GUN126R curve so Carsales/private retail evidence sets the price buckets, not Autotrader. Structured the user's `107` Carsales/private listings into `output/ute_lanes/hilux_sr_gun126r_carsales_private_evidence.csv`, replaced the Hilux grid in `CSV_data/restricted/curves.csv` with Carsales-led conservative anchors, and updated the config notes/validation report to record the source hierarchy: Carsales/private sets buckets, Autotrader is a plotted point-cloud/fit check, and repaired Grays sold rows are the buy-side spread check. Final validation shows `66` Autotrader rows with median Autotrader `$42,975`, median curve-at-point about `$42,396`, and median delta about `-$496`; the `107` Carsales/private rows show median ask `$41,000`, median curve-at-point about `$38,425`, and median delta about `-$1,000`. The main caution is high-km evidence above the current 200k km anchor cap, where 250k/300k km rows can sit well below the grid. 2023/2024 near-new rows stay as future extension review rather than current anchors.
- 2026-05-13: Tightened central repair pricing after sampling current active/sold condition notes. `shared/repair_pricing.py` now splits condition notes on full stops, semicolons, and newlines; counts mixed replacement plus cosmetic surface damage instead of dropping the cosmetic component; caps unknown-photo/inspection boilerplate risk at one buffer after fragment splitting; avoids treating interior seat damage as a body panel; and hard-avoids bare `engine light`, dash warning-light language, head-gasket language, and cooling-system-requires-attention notes. Focused repair tests cover the new parser, mixed-cost, unknown-risk, and hard-stop cases.
- 2026-05-14: Re-triaged stale outstanding notes. Toyota Corolla ZRE182R hatch legacy-branch replacement is resolved at the curve-source level, and 2023/2024 Hilux SR GUN126R extension work is no longer being tracked as actionable because it is outside likely budget. Scheduler reliability remains open. The artifact-footprint issue is now framed as an execution decision: policy, hooks, ignore rules, and backup scripts exist, but high-churn runtime CSVs have not been deliberately untracked.
- 2026-05-14: Repaired the Grays active pipeline contract so operator stages are incremental and idempotent. Link extraction now merges new active links into the unresolved active queue instead of replacing it from one scrape snapshot; detail extraction now exits without touching outputs when there are no pending URLs unless `--all` is explicitly used; active materialization now uses `active_vehicle_links.csv` + `vehicle_static_details.csv` as the active candidate source and treats `vehicle_state.csv` as lifecycle/bid enrichment rather than requiring `state=active`. `active_vehicle_details.csv` is written in governed schema order with `date_sold`, and readiness now validates active rows against the active queue/static details while rejecting terminal state conflicts. Focused pytest, readiness smoke, governance checks, and project-memory checks pass; full pytest was attempted but timed out at 5 minutes and was stopped before completion.
- 2026-05-31: Finished the Hyundai Accent Active RB automatic hatch and iLoad TQ diesel automatic van source slice. The lanes now have explicit allowed-variant/group/universe entries, Hyundai curve-decision notes, canonical-tagging coverage for Accent/iLoad recognition and major exclusions, and valuation cache refresh coverage for newer bid/economics display fields. The current active Accent row is tagged and valued; the only current iLoad row is manual and remains intentionally unclassified for the automatic van lane.
- 2026-06-01: Prepared the residual sold-only repair preview after `bcf409e`. `CSV_data/archives/residual_sold_price_repair_preview_20260601.csv` compares the `139` live URLs that were absent from `CSV_data/archives/sold_cars_rescraped.csv`: `36` rows had clear date-only repairs from the old repaired preview/historical archive, `17` preview dates were rejected as future-looking or less consistent with live row order, and `86` rows needed no merge. Applied only the `36` supported `date_sold` corrections to `CSV_data/scrapers/sold_cars.csv`, mirrored the `3` affected restricted rows, and left all prices/bids unchanged.
- 2026-06-02: Manually checked all `139` residual sold-only URLs against live Grays pages. The check confirmed the broad repair direction but found the 2026-06-01 preview was incomplete: `29` rows needed live-page `Closed` date corrections and `16` rows needed unambiguous live-page `Sold for` price corrections. Wrote `CSV_data/archives/residual_sold_price_manual_check_20260602.csv`, applied the clear corrections to `CSV_data/scrapers/sold_cars.csv`, mirrored the single affected restricted sold row, and left rows without parseable page price/date evidence unchanged.
- 2026-06-02: Simplified the sold categorisation path back toward the original direct model. `scripts/update_bids.py` still treats explicit Grays `Sold for` headings as verified final sale prices, but `scripts/update_master.py` no longer writes or prunes live `sold_price_pending.csv` rows. Any `sold` state without `final_sale_price` is skipped with a clear log message instead of entering a pending queue; ended/referred pages without `Sold for` remain referred/closed rather than becoming sold history.
- 2026-06-06: Fixed referred-history duplicate URLs. `scripts/update_master.py` now merges referred/cancelled rows by URL only and compacts existing referred duplicates during the normal master rebuild, preventing blank-VIN referred rows from being appended beside an existing static row for the same lot. Rebuilt `CSV_data/scrapers/referred_cars.csv` from `6,130` rows / `4,841` unique URLs to `4,841` rows with `0` duplicate URLs; sold and active URL uniqueness stayed clean.
- 2026-06-06: Corrected the scheduled Autotrader scrape scope. The daily scheduler was invoking `autotrader_isolated/scrape_first_page.py` without `--urls-file`, so it silently used the scraper default all-used-cars Melbourne URL; the latest snapshot had `5,367` rows but only `142` curve-tagged rows, with `3,096` rows outside the supported seed makes. `scripts/scheduled_jobs.py` now passes `autotrader_isolated/seed_urls.txt`, and that seed file now covers the supported Ford, Hyundai, Mazda, Mitsubishi, and Toyota makes.
- 2026-06-14: Tightened AI Analysis Autotrader evidence consumption. `pages/6_AI_ANALYSIS.py` now prefers the maintained `autotrader_recent_market_tagged.csv` feed instead of the accumulated `first_page_results.csv`, whose first-seen rows can have stale canonical tags. Autotrader confirmation also treats `suv`/`wagon`/`crossover` body labels as equivalent, restoring current chart points for rows such as Hyundai ix35 where Grays and Autotrader use different body wording.
- 2026-06-14: Tightened Telegram listing alerts to the canonical bid-ready signal. `scripts/ai_listing_valuation.py` now sends new-listing alerts only when `action_label == "Buy"` and sends transition alerts when a previous `Buy` becomes non-Buy; `Watch` rows no longer trigger bid-ready alerts. The generated Telegram alert log/state CSVs are ignored locally while the governed AI valuation baseline files remain trackable.
